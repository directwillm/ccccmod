Commands:

Instant Overload:
	sc.model.player.elementLoad = 1.1
	
	ig.game.spawnEntity(ig.ENTITY.WaveTeleport, ig.game.playerEntity.coll.pos.x, ig.game.playerEntity.coll.pos.y, ig.game.playerEntity.coll.pos.z, {})
	
	
	bomb:
	
	var c = ig.game.spawnEntity(sc.BombEntity, ig.game.playerEntity.coll.pos.x, ig.game.playerEntity.coll.pos.y, ig.game.playerEntity.coll.pos.z+ 100, {});
	c.start(0, ig.game.playerEntity ,0,0);
	c.timer = 2;
	
	
	new sc.EnemyType("arid.mega-laser").load(() => ig.game.spawnEntity("Enemy", ig.game.playerEntity.coll.pos.x, ig.game.playerEntity.coll.pos.y, ig.game.playerEntity.coll.pos.z+100, {
name:"megaLaser",
enemyInfo:{
	group:"",
    party:"",
    type:"arid.mega-laser",
    targetOnSpawn: true}}))
	
var c;new sc.EnemyType("arid.mega-laser").load(() => {
	c = ig.game.spawnEntity("Enemy", ig.game.playerEntity.coll.pos.x, ig.game.playerEntity.coll.pos.y, ig.game.playerEntity.coll.pos.z+1000, {
	name:"megaLaser",
	enemyInfo:{
		group:"",
		party:"",
		type:"arid.mega-laser",
		targetOnSpawn: true}
	});
})